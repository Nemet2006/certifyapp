apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-service
  namespace: certifyapp
spec:
  replicas: 2
  selector:
    matchLabels:
      app: auth-service
  template:
    metadata:
      labels:
        app: auth-service
    spec:
      containers:
        - name: auth-service
          image: ${ECR_REGISTRY}/certifyapp/auth-service:latest
          ports:
            - containerPort: 8081
          envFrom:
            - configMapRef:
                name: certifyapp-config
            - secretRef:
                name: certifyapp-secrets
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8081
            initialDelaySeconds: 45
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8081
            initialDelaySeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: certifyapp
spec:
  selector:
    app: auth-service
  ports:
    - port: 8081
      targetPort: 8081
