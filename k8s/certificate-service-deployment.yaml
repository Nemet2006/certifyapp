apiVersion: apps/v1
kind: Deployment
metadata:
  name: certificate-service
  namespace: certifyapp
spec:
  replicas: 2
  selector:
    matchLabels:
      app: certificate-service
  template:
    metadata:
      labels:
        app: certificate-service
    spec:
      containers:
        - name: certificate-service
          image: ${ECR_REGISTRY}/certifyapp/certificate-service:latest
          ports:
            - containerPort: 8083
          envFrom:
            - configMapRef:
                name: certifyapp-config
            - secretRef:
                name: certifyapp-secrets
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8083
            initialDelaySeconds: 45
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8083
            initialDelaySeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: certificate-service
  namespace: certifyapp
spec:
  selector:
    app: certificate-service
  ports:
    - port: 8083
      targetPort: 8083
